/** @jsxImportSource @emotion/react */
import { wrapper, title } from "./Configurator.styles";

export const Configurator: React.FC = () => {
  return (
    <section css={wrapper}>
      <p css={title}>Toppings! Toppings!</p>
      {/* Todo: Add stuff here */}
    </section>
  );
};
